// node is the engine which healps to run js files

console.log('hello earth');

var a = 4; // (old way of writting )
// after js es6 we use let instead of var  (let ~ var)
let b = '7';
const c = 3.3;

console.log(typeof(b));